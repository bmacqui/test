const cards = document.querySelectorAll(".card")

cards.forEach(card => {
    const width = card.getAttribute("width")
    const height = card.getAttribute("height")

    const backgroundColor = card.getAttribute("backgroundColor")


});